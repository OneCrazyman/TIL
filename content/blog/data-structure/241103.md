---
title: "[Data Structure] 배열과 리스트로 스택 구현 연습"
date: '2024-11-03'
---
```cpp
#include <iostream>
#define MAX 100

struct Node {
	int data;
	Node* next;

	Node(int value) :data(value), next(nullptr) {}
};

class Stack {
private:
	Node* top;
public:
	Stack() :top(nullptr) {}

	bool isEmpty() {
		return (top == nullptr);
	}

	/*bool isFull() {
		return (top == MAX - 1);
	}*/

	void push(int value) {
		//
		Node* node = new Node(value);
		node->next = top;
		top = node;
	}

	int pop() {
		if (isEmpty()) {
			std::cout << "empty\n";
			return -1;
		}
		return arr[top--];
	}

	int peek() {
		if (isEmpty()) {
			return -1;
		}
		return arr[top];
	}

	void display() {
		for (int i = 0; i <= top; i++) {
			std::cout << arr[i] << " ";
		}
	}
};

int main(void) {
	Stack s;

	s.pop();
	s.push(1);
	s.push(2);
	std::cout << s.peek() << "ooo \n";
	s.pop();
	s.display();

	return 0;
}
```